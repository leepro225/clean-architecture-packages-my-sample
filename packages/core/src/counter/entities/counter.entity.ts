export class Counter {
    id: string;
    label: string;
    currentCount: number = 0;
    incrementAmount: number = 1;
    decrementAmount: number = 1;
}

/**
 * 1. 엔티티를 선언한다.
 * 엔티티: 고객으로부터 수집하려는 데이터 유형.
 */